# rust-raytracer
Simple raytracer implemented in Rust, which was made for a school project for second year of my bachelor degree.

// prikaz na spustenie -> cargo run --quiet | Out-File -FilePath img.ppm -Encoding ascii ; ./imgView.exe img.ppm
